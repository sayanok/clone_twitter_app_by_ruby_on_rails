<% content_for :title, "Tweets" %>
<%= render 'form', tweet: @tweet%>

<div id="tweets">
  <% @tweets.each do |tweet| %>
    <%= render tweet %>
    <p>
      <%= link_to tweet.tweet_text, tweet %>
    </p>
  <% end %>
</div>